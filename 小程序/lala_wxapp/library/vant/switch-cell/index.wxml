<van-cell center border="{{border}}" customClass="van-switch-cell" title="{{title}}">
    <van-switch bind:change="onChange" checked="{{checked}}" customClass="van-switch-cell__switch" disabled="{{disabled}}" loading="{{loading}}" size="{{size}}"></van-switch>
</van-cell>
