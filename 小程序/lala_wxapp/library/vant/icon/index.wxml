<view bind:tap="onClick" class="custom-class {{classPrefix}} {{classPrefix}}-{{name}}" style="{{color?'color: '+color+';':''}}{{size?'font-size: '+size+';':''}}{{customStyle}}">
    <view class="van-icon__info" wx:if="{{info!==null}}">{{info}}</view>
</view>
